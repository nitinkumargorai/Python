# when we cutting the slice of the any string then it called slicing

# str[starting_idx : ending_idx],,,,,, ending idx is not included
 
str = "Be kind with everyone"
print(str[0:4]) #in this case it never include fourth number of letter is stopped in 3rd 
print(str[0:]) #it goes to last index
print(str[:7]) #in this case it always start with 0 index 
